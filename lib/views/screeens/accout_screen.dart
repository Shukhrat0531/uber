import 'package:flutter/material.dart';

class AccoutScreen extends StatelessWidget {
  const AccoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('AccoutScreen'),
    );
  }
}